/** The common timeframe types of a period. */
export enum MidaSymbolPeriodTimeframeType {
    /** 1 second. */
    S1 = 1,
    /** 1 minute. */
    M1 = 60,
    /** 5 minutes. */
    M5 = 300,
    /** 15 minutes. */
    M15 = 900,
    /** 30 minutes. */
    M30 = 1800,
    /** 1 hour. */
    H1 = 3600,
    /** 4 hours. */
    H4 = 14400,
    /** 1 day. */
    D1 = 86400,
    /** 1 week. */
    W1 = 604800,
}
