/** The status type of an order. */
export enum MidaBrokerOrderStatusType {
    PENDING = "pending",
    CANCELED = "canceled",
    STARTED = "started",
    REJECTED = "rejected",
    EXPIRED = "expired",
    FILLED = "filled",
}
