export enum MidaBrokerOrderStatus {
    REQUESTED = "requested",
    ACCEPTED = "accepted",
    PENDING = "pending",
    PARTIALLY_FILLED = "partially-filled",
    FILLED = "filled",
    REJECTED = "rejected",
    CANCELLED = "cancelled",
}
