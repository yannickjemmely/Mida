export enum MidaBrokerOrderStatus {
    REQUESTED = "requested",
    REJECTED = "rejected",
    ACCEPTED = "accepted",
    PENDING = "pending",
    CANCELLED = "cancelled",
    PARTIALLY_FILLED = "partially-filled",
    FILLED = "filled",
    EXPIRED = "expired",
}
