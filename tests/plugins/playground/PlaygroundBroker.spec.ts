import { PlaygroundBroker } from "!plugins/playground/PlaygroundBroker";

describe("PlaygroundBroker", () => {
    describe(".login", () => {
        it("returns a PlaygroundBrokerAccount instance", () => {
            expect(true).toBe(true);
        });
    });
});
